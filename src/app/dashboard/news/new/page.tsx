import{requirePermission}from"@/lib/auth/dal";import{AdminPage}from"../../components/admin-page";import{createNewsAction}from"../actions";import{NewsForm}from"../news-form";
export default async function NewNewsPage({searchParams}:{searchParams:Promise<{error?:string}>}){const session=await requirePermission("content:write");const state=await searchParams;return <AdminPage title="新增新闻资讯" eyebrow="NEW STORY" tenant={session.tenant.name}>{state.error&&<div className="form-error">{state.error==="duplicate"?"该 URL 别名已存在，请更换后再试。":"请检查必填项和 URL 格式。"}</div>}<NewsForm action={createNewsAction} submitLabel="创建资讯"/></AdminPage>}
