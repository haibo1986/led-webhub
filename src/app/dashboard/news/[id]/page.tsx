import{notFound}from"next/navigation";import{requirePermission}from"@/lib/auth/dal";import{getDb}from"@/lib/db";import{AdminPage}from"../../components/admin-page";import{ConfirmForm}from"../../components/confirm-form";import{deleteNewsAction,setNewsStatusAction,updateNewsAction}from"../actions";import{NewsForm}from"../news-form";
export default async function EditNewsPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{saved?:string;created?:string;error?:string}>}){const session=await requirePermission("content:write");const{id}=await params;const state=await searchParams;const item=await getDb().newsPost.findFirst({where:{id,tenantId:session.tenantId,deletedAt:null},include:{translations:true}});if(!item)notFound();const zh=item.translations.find(t=>t.locale==="ZH_CN");const en=item.translations.find(t=>t.locale==="EN");const update=updateNewsAction.bind(null,id);const zhAction=setNewsStatusAction.bind(null,id,"ZH_CN",!zh?.isPublished);const enAction=setNewsStatusAction.bind(null,id,"EN",!en?.isPublished);const remove=deleteNewsAction.bind(null,id);const actions=<div className="locale-publish-actions"><form action={zhAction}><button className={zh?.isPublished?"is-live":""}><b>中文</b><span>{zh?.isPublished?"已上线 · 点击下线":"点击发布"}</span></button></form><form action={enAction}><button className={en?.isPublished?"is-live":""}><b>English</b><span>{en?.isPublished?"Live · Unpublish":"Publish"}</span></button></form><ConfirmForm action={remove} message="确定删除这篇资讯？删除后将移入归档。"><button className="delete-content"><b>删除</b><span>移入归档</span></button></ConfirmForm></div>;return <AdminPage title={`编辑 · ${zh?.title??item.slug}`} eyebrow="NEWS EDITOR" tenant={session.tenant.name} action={actions}>{(state.saved||state.created)&&<div className="save-notice">资讯内容已保存。</div>}{state.error&&<div className="form-error">{state.error==="duplicate"?"该 URL 别名已存在，请更换后再试。":"请检查必填项和 URL 格式。"}</div>}<NewsForm action={update} submitLabel="保存修改" value={{slug:item.slug,category:item.category,titleZh:zh?.title??"",titleEn:en?.title??"",excerptZh:zh?.excerpt??"",excerptEn:en?.excerpt??"",bodyZh:zh?.body??"",bodyEn:en?.body??""}}/></AdminPage>}
